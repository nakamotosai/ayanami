# memory-lite

用途：轻量记忆维护（写入 daily、快速 grep/搜索、生成摘要）。

依赖：`qmd`（已安装），集合默认 `memory`。

常用：
- 追加一条 daily：`bash scripts/memory_add_daily.sh "文本"`
- 搜索：`bash scripts/memory_search.sh "关键词"`
- 重新索引：`bash scripts/memory_update.sh`
