# MCP Tesseract OCR Server

MCP图像文字识别工具 - 提供强大的OCR文字提取功能

## 功能特性

- 🔍 **文字识别**: 从图片中提取文字内容
- 🌍 **多语言支持**: 支持中文、英文、日文等多种语言
- 📁 **批量处理**: 可同时处理多个图像文件
- 🎨 **图像预处理**: 自动优化图像以提高识别准确率
- 📊 **置信度分析**: 提供识别结果的置信度信息

## 使用场景

- 📝 **文档文字提取**: 从图片、文档中提取文字
- 🏷️ **图片文字识别**: 识别图片中的文字内容
- 📋 **批量处理**: 处理大量图片的文字识别任务
- 🔤 **多语言处理**: 支持中英文混合识别

## 可用工具

### 1. extract_text_from_image
从单个图片中提取文字

```javascript
extract_text_from_image({
  image_path: "/path/to/image.jpg",
  language: "chi_sim+eng"  // 简体中文+英文
})
```

**参数**:
- `image_path`: 图片文件路径
- `language`: OCR语言代码 (默认: "chi_sim+eng")

### 2. extract_text_from_images_batch
批量处理多个图片的文字识别

```javascript
extract_text_from_images_batch({
  image_paths: ["/path/to/image1.jpg", "/path/to/image2.png"],
  language: "chi_sim+eng",
  output_format: "json"
})
```

**参数**:
- `image_paths`: 图片路径数组
- `language`: OCR语言代码
- `output_format`: 输出格式 (json/text/csv)

### 3. list_ocr_languages
列出所有可用的OCR语言代码

## 支持的语言

| 语言代码 | 语言名称 | 支持状态 |
|---------|---------|----------|
| `eng` | 英语 | ✅ |
| `chi_sim` | 简体中文 | ✅ |
| `chi_tra` | 繁体中文 | ✅ |
| `jpn` | 日语 | ✅ |
| `kor` | 韩语 | ✅ |
| `fra` | 法语 | ✅ |
| `deu` | 德语 | ✅ |
| `spa` | 西班牙语 | ✅ |
| `rus` | 俄语 | ✅ |
| `ara` | 阿拉伯语 | ✅ |

## 安装和配置

此技能已预装在工作区中，通过 MCP 协议提供：

```json
{
  "mcpServers": {
    "tesseract-ocr": {
      "command": "node",
      "args": [
        "/home/ubuntu/.openclaw/workspace/mcp-tesseract-server/index.js"
      ]
    }
  }
}
```

## 依赖包

- **tesseract.js**: OCR核心引擎
- **sharp**: 图像预处理库
- **@modelcontextprotocol/sdk**: MCP协议支持

## 使用示例

### 示例1: 识别B站视频标题
```bash
# 识别图片中的中文和英文内容
extract_text_from_image({
  image_path: "/tmp/line-media-599888580492132558-1770392762456.jpg",
  language: "chi_sim+eng"
})
```

### 示例2: 批量处理
```bash
# 处理多个图片文件
extract_text_from_images_batch({
  image_paths: ["/image1.jpg", "/image2.png", "/image3.jpg"],
  language: "chi_sim",
  output_format: "json"
})
```

## 性能特点

- ⚡ **快速识别**: 基于Tesseract LSTM引擎
- 🎯 **高准确率**: 图像预处理+智能识别
- 🔄 **批量处理**: 支持多文件并行处理
- 📈 **详细输出**: 包含置信度、词数等信息