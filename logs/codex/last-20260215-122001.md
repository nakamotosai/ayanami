已完成，且仅基于 Round 136 的限定来源边界进行处理（`src-*.txt/.url`，实际均无文件实体）。

已追加写入：
- `memory/learn/polymarket_/kb.md:4429`
- `memory/learn/polymarket_/questions.md:1209`

本轮写入内容包含并覆盖：
- 套利玩法
- 交易策略
- 风控
- 常见坑
- 合规与道德边界（仅风险提醒，不提供非法指导）

并已满足：
- 只追加、不重写整文件
- 每个关键结论带 `(Round 136 Source N)` 标注
- 去重导向（延续既有框架，仅补 Round 136 的新增约束与字段规范）

**本轮摘要（用于 Telegram，12 条）**
1. Round 136 在 `src-*.txt` 范围 0 命中，`src_txt_count=0`。  
2. Round 136 在 `src-*.url` 范围 0 命中，`src_url_count=0`。  
3. 因双空证据，本轮无任何可新增的市场事实层知识。  
4. 本轮继续执行“空证据禁执行”，禁止输出可落地套利细节。  
5. 玩法层新增约束：条目需绑定 `round_id=136` 与失败态签名，防止历史模板混入。  
6. 策略层继续冻结为“证据修复+流程治理”，禁止参数化交易建议。  
7. 新增 URL 缺失硬回退：`src_url_count=0` 时强制 `publish_scope=research_only`。  
8. 风控维持最高等级：`evidence_state=failed` + `execution_state=frozen`。  
9. 新增冻结态十字段快照建议，缺项或冲突即熔断阻断下游。  
10. 常见坑：把轮次推进误判为证据恢复。  
11. 常见坑：URL 缺失下先写策略参数草稿，造成不可追溯且易被误用。  
12. 合规边界重申：仅提供研究与风控信息，不提供规避监管/KYC/AML 的指导。