已完成，均为“只追加”写入：

- `memory/learn/polymarket_/kb.md`：已追加 **Round 151 增量沉淀**（含 套利玩法 / 交易策略 / 风控 / 常见坑 / 合规与道德边界），并附 `(Round 151 Source N)` 标注。
- `memory/learn/polymarket_/questions.md`：已追加 **10 条未解问题**（编号 `1339-1348`，按优先级 P0/P1）。

以下是本轮可回传 Telegram 的摘要（15 条）：

1. Round 151 在限定输入 `src-*.txt/.url` 下双侧无文件实体，`src_txt_count=0`、`src_url_count=0`。  
2. 本轮不允许新增任何盘口结构、价差窗口、滑点路径、时序等市场事实结论。  
3. 本轮无法建立“结论 -> URL”追溯链，参数化执行内容（仓位/阈值/触发器）全部冻结。  
4. 本轮仅可沉淀证据治理、流程阻断、风险边界、合规提醒。  
5. 套利玩法层继续执行“无证据即不可交易”，禁止输出可执行套利步骤。  
6. 新增玩法门禁建议：`tradability=none` + `playbook_state=frozen` + `freeze_reason=missing_src_evidence_round_151` 必须同时存在。  
7. 交易策略层仅允许补证任务与修复计划，不允许收益目标和进出场参数。  
8. 新增策略双闸建议：`candidate_entry=false` + `dispatch_eligibility=denied`，且 `draft_class=non_executable`。  
9. 风控维持最高等级：`evidence_state=failed`、`execution_state=frozen`、`publish_scope=research_only`。  
10. 新增放行校验单建议：发布前核对 `round_id/src计数/execution_state/publish_scope`，不一致即熔断。  
11. 连续空源需升级告警并绑定 `owner/sla_deadline/escalation_path`，防止失败态常态化。  
12. 常见坑：误把 `search-*.json`、`urls.txt` 当作 `src` 证据。  
13. 常见坑：URL 缺失时先写可执行策略草稿，造成伪交易信号。  
14. 常见坑：只冻结执行态，未联动候选池与外发资格，导致侧路泄漏。  
15. 合规边界：仅提供合法合规的研究与风控提醒，不提供规避监管/KYC/AML/平台规则的指导。