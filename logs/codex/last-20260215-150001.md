已完成本轮写入（只追加，未重写）：

- `memory/learn/polymarket_/kb.md:4983`
- `memory/learn/polymarket_/questions.md:1359`

**本轮摘要（用于 Telegram，15 条）**
1. Round 152 在限定输入 `src-*.txt/.url` 下双空：`src_txt_count=0`、`src_url_count=0`。  
2. 本轮无法新增任何盘口、价差、深度、时序等市场事实知识。  
3. 本轮无法建立“结论 -> URL”追溯链，参数化执行信息全部冻结。  
4. 知识沉淀仅允许流程约束、风险提示、合规边界，不允许实盘细节。  
5. 套利玩法层继续执行“无证据即不可交易”。  
6. 新增玩法冻结字段建议：`tradability=none`、`playbook_state=frozen`、`freeze_reason_code=ROUND_152_SRC_EMPTY`。  
7. 交易策略层仅允许补证/修复任务单，不得写收益目标、阈值、仓位、时序。  
8. 新增策略双闸建议：`candidate_entry=false` + `external_dispatch=false`，并强制 `draft_class=non_executable`。  
9. 风控维持最高等级：`evidence_state=failed`、`execution_state=frozen`、`publish_scope=research_only`。  
10. 新增风控审计最小闭环：发布前校验 `round_id/src_txt_count/src_url_count/evidence_state/execution_state/publish_scope/check_time`。  
11. 明确解冻唯一入口：仅当 `src_txt_count>0 && src_url_count>0` 且文本-URL 一一映射完成。  
12. 常见坑：把 `search-*.json` / `urls.txt` 误当 `src` 证据。  
13. 常见坑：URL 证据缺失时先写执行参数，导致不可追溯内容被误读为交易信号。  
14. 常见坑：只冻结执行态，不关闭候选与外发通道，导致侧路执行风险。  
15. 合规边界重申：不提供规避监管/KYC/AML/平台规则的指导；证据恢复后先过条款、地区准入、数据许可、税务、AML、披露核验。  

如果你要，我可以下一轮把这 15 条压缩成更短的“超精简版 8 条”用于手机通知。