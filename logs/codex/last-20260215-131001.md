已完成本轮写入（仅追加，未重写）：

- `memory/learn/polymarket_/kb.md`：已追加 **Round 141 增量沉淀**，覆盖套利玩法/交易策略/风控/常见坑/合规与道德边界，并在关键结论末尾加了 `(Round 141 Source N)`。
- `memory/learn/polymarket_/questions.md`：已追加 **未解问题 1249-1258**（按 P0/P1 优先级）。

本轮 Telegram 摘要（14 条）：
1. Round 141 在限定输入 `src-*.txt/.url` 下双侧 0 命中，证据链仍为空。  
2. `src_txt_count=0`，无法新增盘口、成交、价差、滑点、时序类事实知识。  
3. `src_url_count=0`，无法建立“结论 -> URL”追溯链。  
4. 本轮继续执行“空证据禁执行”，不输出可执行套利细节。  
5. 新增玩法侧“三键锁”建议：`tradability=none`、`execution_candidate=false`、`publish_scope=research_only`。  
6. 策略侧仅允许“证据修复计划 + 流程治理动作”，禁止收益/仓位/阈值/时序参数。  
7. 新增 URL 零命中硬回退：`src_url_count=0` 时强制只读并联动 `candidate_entry=false`、`external_dispatch=false`。  
8. 风控维持最高冻结：`evidence_state=failed`、`execution_state=frozen`。  
9. 新增“冻结态十三字段快照”建议，用于一致性校验与熔断。  
10. 新增“证据计数驱动解冻”约束：禁止人工改单字段直接解冻。  
11. 常见坑：把“轮次完成”误判成“证据恢复”。  
12. 常见坑：URL 缺失时提前写参数草稿，造成伪可执行信号。  
13. 常见坑：只看 `execution_state`，不校验 `candidate_entry/external_dispatch` 导致侧路放行。  
14. 合规边界重申：只提供合法研究与风控信息，不提供规避监管/KYC/AML/平台规则指导。