已完成，均为**追加写入**（未重写）：

- `memory/learn/polymarket_/kb.md`：已追加 `Round 89 增量沉淀`，包含  
`套利玩法 / 交易策略 / 风控 / 常见坑 / 合规与道德边界` 五部分，并在关键结论末尾标注 `(Round 89 Source N)`。
- `memory/learn/polymarket_/questions.md`：已按优先级追加 `741-750` 共 10 条未解问题，均带 `(Round 89 Source N)` 标注。

本轮可用输入结论：`round-89` 下无 `src-*.txt`、无 `src-*.url`（双空），仅有空的 `search-*.json` 和 `urls.txt`，因此本轮继续是“证据治理增量”，不是“交易知识增量”。

**本轮摘要（Telegram，15条）**
1. Round 89 在限定输入下仍是双空：`src-*.txt=0`、`src-*.url=0`。  
2. 连续空源轮次由 79 提升到 80。  
3. 本轮定位为“证据治理停机复盘闸门与恢复前双签硬封印”阶段。  
4. 玩法层继续冻结：禁止输出可执行套利步骤（跨市场对冲、YES+NO 合成、负风险组合等）。  
5. 新增玩法门禁：未双签（数据链路负责人+策略负责人）不得入执行候选池。  
6. 新增配额控制：连续 80 轮空源时，玩法新增/升级配额强制为 0。  
7. 策略层继续仅允许排障输出，禁止收益判断和参数迭代。  
8. 字段白名单收紧：仅允许 `evidence_status/risk_level/repair_progress/acceptance_result/rollback_state`。  
9. 恢复顺序硬化：必须先做证据验收与抽样一致性，再进入策略评审。  
10. 风控维持最高级：未满足“四联门禁”（非零双侧+可追溯+配对+可定位）前禁止实盘导向入库。  
11. 新增触发器：若下一轮仍双空，必须执行强制复盘（根因、SLA、时限、验收清单）。  
12. 常见误区：把“目录存在/单轮文件出现”误判为“证据恢复”。  
13. 常见误区：以“模板演示”名义写参数和收益，导致研究文本被误当交易指令。  
14. 合规边界不变：不提供规避监管、规避 KYC、绕平台规则等指导。  
15. 已在 `questions.md` 追加 10 条高优先级问题，覆盖根因、门禁、假恢复熔断、双签机制与合规复核。