已完成，且只做了“追加写入”。

- 已追加到 `memory/learn/polymarket_/kb.md`：新增 `Round 147 增量沉淀` 完整分节（套利玩法/交易策略/风控/常见坑/合规与道德边界/输入边界复核）。
- 已追加到 `memory/learn/polymarket_/questions.md`：新增 `1299-1308` 共 10 条未解问题，按 `P0/P1` 排序。
- 所有关键结论末尾都带了来源标注：`(Round 147 Source N)`。
- 本轮限定输入下，`round-147` 未发现任何 `src-*.txt` 或 `src-*.url` 文件实体，因此结论保持“证据双空 -> 执行冻结”。
- 内容严格保持“只追加、不重写整文件”。

本轮摘要（Telegram 用，14 条）：
1. Round 147 在 `src-*.txt` 侧命中为 0，事实层无新增。  
2. Round 147 在 `src-*.url` 侧命中为 0，追溯链无法建立。  
3. 本轮继续执行“证据双空即冻结执行”的总原则。  
4. 套利玩法层明确“无证据即不可交易”，禁止输出可执行套利细节。  
5. 新增玩法门禁：`tradability=none + playbook_state=frozen + evidence_contract` 三元签名缺一不可。  
6. 交易策略层仅允许补证计划与修复计划，不允许收益率/仓位/阈值/时序参数。  
7. 新增候选准入约束：需 `src_txt_count>0 && src_url_count>0 && mapping_proof_id` 才能进入候选。  
8. 风控维持最高级：`evidence_state=failed`、`execution_state=frozen`、`publish_scope=research_only`。  
9. 新增“状态迁移凭证锁”：解冻必须有 `transition_proof_id`，禁止人工改单字段解冻。  
10. 新增空源审计最小集字段，便于回放追责与误放行定位。  
11. 常见坑一：把 `search-*.json/urls.txt` 误当作 `src` 合法证据。  
12. 常见坑二：URL 缺失时先写参数草稿，导致不可追溯文本被误读为可执行信号。  
13. 常见坑三：冻结执行态但没有迁移凭证，留下绕过门禁空间。  
14. 合规边界重申：仅输出合法研究与风控，不提供规避监管/KYC/AML/平台规则的指导。