已完成，本轮已按“只追加”写入：

- `memory/learn/polymarket_/kb.md`：追加 **Round 149 增量沉淀**
- `memory/learn/polymarket_/questions.md`：追加 **未解问题 1319-1328（按优先级）**

本轮严格仅使用 `round-149` 下 `src-*.txt/.url` 作为来源边界；实际校验结果为 `src_txt_count=0`、`src_url_count=0`，并已在关键结论后标注 `(Round 149 Source N)`。

**本轮摘要（Telegram 版，12 条）**
1. Round 149 在 `src-*.txt` 无命中，无法新增任何市场事实层知识。  
2. Round 149 在 `src-*.url` 无命中，无法建立“结论 -> URL”追溯链。  
3. 本轮继续维持“证据双空 -> 执行冻结”的研究态。  
4. 套利玩法层继续禁止任何可执行细节（搬砖/对冲/多腿/抢跑等）。  
5. 新增玩法门禁：`tradability=none + playbook_state=frozen + execution_reachability=blocked_by_empty_sources` 缺一不可。  
6. 交易策略层仅允许补证与链路修复任务，不允许收益目标、仓位、阈值、时序参数。  
7. 新增候选池门禁矩阵：`src_txt_count=0 || src_url_count=0` 时强制不可候选、不可调度。  
8. 风控继续最高级：`evidence_state=failed + execution_state=frozen + publish_scope=research_only`。  
9. 新增“状态指纹”建议：发布前校验 `state_lock_hash` 与关键状态字段一致性。  
10. 新增“解冻双证据硬闸”：仅在文本与 URL 双恢复且完成一一映射后才可解冻。  
11. 常见坑聚焦三类：来源边界越权、URL 缺失下写可执行草稿、冻结态未联动候选/外发通道。  
12. 合规边界再次强调：仅做合法研究与风控提示，不提供规避监管/KYC/AML 的指导。