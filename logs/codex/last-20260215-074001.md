已完成，且仅做了追加写入：

- 已追加到 `memory/learn/polymarket_/kb.md`
- 已追加到 `memory/learn/polymarket_/questions.md`（新增 931-940）

本轮严格按你给的边界执行：仅基于 `round-108/src-*.txt` 与 `src-*.url`，结论是两者均 0 命中，因此未引入任何执行型套利细节。

**Round 108 回传摘要（Telegram 版，15 条）**
1. Round 108 在 `src-*.txt` 侧 0 命中，缺失正文证据。  
2. Round 108 在 `src-*.url` 侧 0 命中，缺失 URL 追溯链。  
3. 本轮无 `txt/url` 配对证据，不能新增任何可执行交易结论。  
4. 套利玩法维持冻结：禁止补写跨市场对冲、YES/NO 合成、事件篮子、负风险组合执行细节。  
5. 新增玩法“代际锁”：要求 `evidence_generation=108` + `tradability=blocked` + `publish_scope=research_only` + `execution_candidate=false` 联动。  
6. 交易策略仅允许排障与验收模板，不允许收益率、仓位、阈值、下单顺序等字段。  
7. 新增策略双门禁：`execution_state=frozen` 与 `strategy_publish_channel=blocked` 必须同时成立。  
8. 即便后续首次恢复非空 `src-*`，也仅可进入观察态，不可直接解冻执行。  
9. 风控维持最高级：证据输入不可用，所有交易导向主张默认高不确定并拒绝入库。  
10. 入库硬门禁继续生效：正文非空、URL非空、一一配对、段落定位、责任签收缺一不可。  
11. 常见坑：把“轮次推进”误判为“证据恢复”，导致提前讨论执行路径。  
12. 常见坑：复用历史模板冒充本轮证据，忽略本轮配对缺失。  
13. 常见坑：研究文本混入执行语义，被下游误当交易指令。  
14. 合规边界重申：只提供合法研究/风控/排障，不提供规避监管或规避 KYC 指导。  
15. questions 已新增 10 条高优先未解项（931-940），重点聚焦根因定位、双空稳态治理、硬门禁工程化、合规放行签收链。