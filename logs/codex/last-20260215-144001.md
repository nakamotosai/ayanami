已完成并已写入：

- `memory/learn/polymarket_/kb.md`：追加 `Round 150 增量沉淀`（含套利玩法/交易策略/风控/常见坑/合规与道德边界），且关键结论均带 `(Round 150 Source N)`。
- `memory/learn/polymarket_/questions.md`：追加未解问题 `1329-1338`（按 P0/P1 优先级），均带 `(Round 150 Source N)`。

本轮用于沉淀的事实前提：`round-150` 下未发现任何 `src-*.txt/.url` 文件，故本轮仅能新增“证据治理、冻结执行、风险与合规边界”类结论，不能新增可执行套利参数。

**本轮摘要（用于 Telegram，12 条）**
1. Round 150 仍是双空源：`src_txt_count=0`、`src_url_count=0`，无可验证市场事实新增。  
2. 本轮继续执行“无证据即不可交易”，禁止输出任何可执行套利细节。  
3. 新增玩法约束：`tradability=none + unfreeze_key=src_txt>0&src_url>0` 双钥缺一不可。  
4. 策略层仅允许补证与修复计划，不允许收益目标、仓位、阈值、时序参数。  
5. 新增候选池四状态联动：`candidate_entry=false`、`strategy_class=non_executable`、`dispatch_eligibility=denied`、`review_mode=evidence_repair_only`。  
6. 风控维持最高等级：`evidence_state=failed`、`execution_state=frozen`、`publish_scope=research_only`。  
7. 新增“冻结态迁移审计号”要求：解冻尝试必须有 `state_transition_ticket` 且证据计数变化可验。  
8. 新增“连续空源疲劳防线”：要求提升检查频率并记录 `owner/sla_deadline/escalation_level`。  
9. 常见坑 1：把目录中其他文件误当 `src` 证据，导致输入边界越权。  
10. 常见坑 2：`src-*.url` 缺失时仍写参数化草稿，易被误读成交易信号。  
11. 常见坑 3：只冻结执行状态，不联动候选池/外发资格，导致侧路入执行链路。  
12. 合规边界重申：只提供合法合规风控研究，不提供绕监管/KYC/AML/平台规则的指导。