已完成本轮追加写入：

- `memory/learn/polymarket_/kb.md` 已追加 Round 117 增量沉淀（仅追加，未重写）。
- `memory/learn/polymarket_/questions.md` 已追加 1021-1030 条未解问题（仅追加，未重写）。
- 所有关键结论均已带 `(Round 117 Source N)` 标注。
- 本轮仅使用了 `round-117` 目录下现有材料（`search-1..5.json`、`urls.txt`）。

本轮摘要（用于 Telegram，14 条）：
1. Round 117 未产出 `src-*.txt/.url`，仅有 `search-1..5.json` 与 `urls.txt` 六个文件。  
2. 六个文件均为 0 字节，属于“抓取壳文件”而非可用证据。  
3. 本轮无法建立“结论 -> URL”追溯链，交易执行知识继续冻结。  
4. 套利玩法层继续禁执行：不输出跨市场对冲、组合配平、抢跑成交等细节。  
5. 交易策略层继续禁参数：不输出收益率目标、仓位、阈值、撤单/重试规则。  
6. 新增“壳文件防误判”要求：文件存在但为空时必须标记 `evidence_state=failed`。  
7. 风控维持最高级：证据供应链不可用时，交易导向文本默认拒绝入库。  
8. 入库硬门禁继续生效：正文非空、URL 非空、可配对、可定位、责任签收缺一不可。  
9. 新增“空壳即失败”校验：仅发现 0 字节壳文件时自动阻断并留审计记录。  
10. 常见坑 1：把“有文件”误判为“有证据”，导致伪恢复。  
11. 常见坑 2：复用旧模板执行参数，导致空证据文本被误读为交易建议。  
12. 常见坑 3：只看 `search` 文件，不核验 `src` 正文与 URL 文件。  
13. 合规边界重申：仅提供合法风控与流程治理，不提供规避监管/KYC/AML 的指导。  
14. 新增未解问题 10 条（1021-1030）：聚焦空壳根因定位、熔断机制、同事务落盘、状态机一致性与告警升级。