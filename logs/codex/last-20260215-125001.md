已完成追加写入（仅追加、未重写）：

- `memory/learn/polymarket_/kb.md`
- `memory/learn/polymarket_/questions.md`

本轮（Round 139）摘要（可直接回传 Telegram）：

1. Round 139 在限定证据范围内，`src-*.txt` 命中数为 0，无法新增任何市场事实层结论。  
2. Round 139 在限定证据范围内，`src-*.url` 命中数为 0，无法建立“结论 -> URL”追溯链。  
3. 双空（txt/url 均为空）状态被继续确认为执行冻结态，只允许研究流程和证据治理内容。  
4. 套利玩法层继续执行“空证据禁执行”，拒绝任何可落地的套利参数与动作细节。  
5. 新增玩法字段硬白名单：仅允许失败态与修复进度字段进入知识库。  
6. 新增 `evidence_signature=src_txt_count:0|src_url_count:0` 约束，用于防止模板污染和字段拼接绕过。  
7. 交易策略层继续限制为“证据修复 + 流程治理”，禁止收益目标、阈值、时序等执行参数。  
8. 新增“解冻双阈值门”：必须同时满足 `src_txt_count>0`、`src_url_count>0` 且完成逐条映射。  
9. 风控维持最高等级：双空即 `evidence_state=failed` 与 `execution_state=frozen`。  
10. 新增“状态机防篡改”要求：禁止仅改单字段实现解冻，状态迁移必须由证据计数变化触发。  
11. 新增“映射覆盖率”门禁建议：解冻前要求 `mapping_coverage=100%`，否则自动回退 `research_only`。  
12. 常见坑更新：轮次推进被误判为证据恢复，仍是本轮最大误用风险。  
13. 常见坑更新：URL 缺失时提前写仓位/收益参数，会形成不可追溯的伪可执行文本。  
14. 常见坑更新：只看失败态不看签名，可能被历史模板拼接绕过冻结门禁。  
15. 合规边界重申：仅提供合法研究与风控信息，不提供规避监管/KYC/AML/平台规则的指导。  
16. `questions.md` 已续号追加 `1229-1238`，按优先级补齐根因定位、原子提交、冻结联动、防篡改、解冻验收、签名治理等未解问题。