已完成本轮写入（仅追加，未重写）：

- 已追加到：`memory/learn/polymarket_/kb.md`
- 已追加到：`memory/learn/polymarket_/questions.md`
- 轮次：Round 125
- 输入边界：仅按 `memory/learn/polymarket_/sources/round-125/src-*.txt` 与 `src-*.url` 处理；两者均无文件命中（双空）

**本轮 Telegram 摘要（14 条）**
1. Round 125 在限定输入下 `src-*.txt` 0 命中，无法新增盘口/成交/价差等事实层知识。  
2. Round 125 在限定输入下 `src-*.url` 0 命中，无法建立“结论 -> URL”追溯链。  
3. 本轮仅允许沉淀证据治理、流程阻断、风险提示、合规边界；执行层知识继续冻结。  
4. 套利玩法继续“空源禁执行”：禁止输出跨市场对冲、多腿配平、负风险拼接等执行细节。  
5. 新增玩法“最小披露”约束：空源轮次只允许 `evidence_state/block_reason/owner/next_checkpoint/ticket_id`。  
6. 交易策略继续仅限“证据修复+流程治理”，禁止收益目标、仓位阈值、撤单重试等参数化内容。  
7. 新增“轮次隔离签名”：策略条目需显式绑定 `round=125` 与 `evidence_state=failed`，防止历史内容冒充本轮证据。  
8. 风控维持最高级：`src_txt_count=0 && src_url_count=0` 时默认 `execution_state=frozen`。  
9. 新增“发布前双检”：必须同时通过“本轮 src 非空 + URL 可追溯”，任一失败即阻断发布。  
10. 新增“空源连续性指纹”：固定记录 `round_id/src_txt_count/src_url_count/evidence_state/check_time`。  
11. 常见坑：误把 `search-*.json/urls.txt` 当作可用证据，忽略本轮有效输入仅 `src-*.txt/.url`。  
12. 常见坑：复用未绑定本轮编号的旧结论，造成证据代际污染。  
13. 常见坑：空源轮次保留参数占位字段，被下游误读为执行信号。  
14. 合规边界重申：不提供规避监管/KYC/AML/地区限制/平台规则的指导；证据恢复后仍需完成条款、准入、数据许可、税务、AML、信息披露核验后再谈执行。  

如果你要，我下一步可以直接做 Round 126，同样按这个模板追加。