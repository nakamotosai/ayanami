已完成本轮追加写入（仅追加、未重写）：

- 已追加到 `memory/learn/polymarket_/kb.md`：`Round 143 增量沉淀`（含套利玩法/交易策略/风控/常见坑/合规与道德边界，且每条关键结论均带 `(Round 143 Source N)` 标注）。
- 已追加到 `memory/learn/polymarket_/questions.md`：`1259-1268` 共 10 条未解问题（按优先级 P0/P1）。

本轮摘要（用于 Telegram，12 条）：
1. Round 143 在限定输入 `src-*.txt` 下命中为 0，无法新增市场事实层知识。  
2. Round 143 在限定输入 `src-*.url` 下命中为 0，无法建立“结论 -> URL”追溯链。  
3. 双空联合结论：本轮仅可沉淀流程治理/风险边界，执行层知识继续冻结。  
4. 套利玩法层维持“空证据禁执行”，不输出可执行多腿/搬砖/抢跑细节。  
5. 新增玩法约束：空源条目需带 `evidence_ref=none` 与 `tradability=none`。  
6. 交易策略层仅允许“证据修复计划”，禁止收益目标、仓位、阈值、时序参数。  
7. 新增策略防误发：`src_url_count=0` 时强制 `draft_class=non_executable` 且 `external_dispatch=false`。  
8. 风控维持最高冻结：`src_txt_count=0 && src_url_count=0` => `evidence_state=failed` + `execution_state=frozen`。  
9. 新增“失败态四联锁”：`evidence_state`、`execution_state`、`candidate_entry`、`publish_scope` 必须同检同存。  
10. 常见坑：把“轮次推进”误判为“数据恢复”，或在 URL 缺失时先写参数化策略草稿。  
11. 合规边界：仅提供合法研究/风控信息，不提供规避监管、绕过 KYC/AML 或平台规则的指导。  
12. questions.md 新增 10 个待解问题，聚焦根因定位、URL 同事务落盘、四联锁工程化、参数越界扫描与合规放行链路。