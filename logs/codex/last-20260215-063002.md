已完成，且仅做了**追加**写入：

- 已追加到 `memory/learn/polymarket_/kb.md`（Round 101 增量沉淀，含：套利玩法/交易策略/风控/常见坑/合规与道德边界/输入边界复核）
- 已追加到 `memory/learn/polymarket_/questions.md`（编号 `861-870`，按优先级）

本轮严格基于你限定的来源路径（`round-101/src-*.txt` 与 `round-101/src-*.url`）。当前两类文件均为空命中，因此所有关键结论都以“证据治理、风控约束、合规边界”为主，并在结尾加了 `(Round 101 Source N)` 标注。

**本轮摘要（Telegram 回传，12 条）**
1. Round 101 限定输入下 `src-*.txt` 为空（`txt_count=0`），无可提炼正文证据。  
2. Round 101 限定输入下 `src-*.url` 为空（`url_count=0`），无法建立 URL 追溯链。  
3. 本轮结论继续归入“证据治理增量”，不归入“交易执行知识增量”。  
4. 套利玩法侧继续冻结，不允许补写跨市场对冲/组合配平等可执行步骤。  
5. 新增玩法一致性快照要求：`evidence_state=missing`、`pairing_rate=0`、`publish_scope=research_only`、`tradability=blocked`。  
6. 新增玩法“解冻资格黑名单”：空源轮次出现步骤化执行文本即入黑名单并重审。  
7. 交易策略侧继续“仅排障输出”，禁止收益判断与参数迭代。  
8. 新增策略一致性快照联动：`execution_state=frozen`、`strategy_publish_channel=blocked`、`execution_candidate=false`、`evidence_snapshot_round=101` 缺一不可。  
9. 新增策略侧黑名单规则：出现收益率/仓位/阈值/优先级/下单顺序字段即回退研究态并审计。  
10. 风控继续维持最高证据风险等级，实盘导向结论入库硬门禁不放松。  
11. 常见坑仍是把 `search-*.json` / `urls.txt` 误当证据恢复，以及“冻结标签存在但通道未封”的半冻结状态。  
12. 合规边界保持不变：只提供合法研究与风控建议，不提供规避监管、绕过平台规则或规避 KYC 的指导。