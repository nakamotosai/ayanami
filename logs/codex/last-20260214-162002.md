已完成，且均为“只追加”写入：

- `memory/learn/polymarket_/kb.md`
- `memory/learn/polymarket_/questions.md`

本轮用于沉淀的事实基础是：`round-16` 未出现任何 `src-*.txt/.url`，目录仅有 0 字节 `search-1..5.json` 与 `urls.txt`，因此只能新增“证据缺失下的流程/风控/边界”类结论，未新增可执行套利参数。

**Round 16 Telegram 摘要（12 条）**
1. Round 16 未生成 `src-*.txt/.url`，不满足最小证据条件，不能新增市场实证结论。  
2. 本轮目录文件全部为 0 字节，属于证据链不可用，不是“信息少”。  
3. 连续空源已到第 8 轮，问题从单轮异常升级为系统性中断。  
4. 套利玩法层继续冻结：跨平台对冲、YES+NO 合成、负风险组合均不得新增细节。  
5. 本轮玩法仅允许“证据状态记录+排障跟踪”，禁止可执行步骤输出。  
6. 交易策略层升级为“禁策略结论输出”：无 `src-*` 时只允许故障报告。  
7. 恢复策略迭代前置条件：至少 1 对非零且可映射的 `src-*.txt/.url`。  
8. 风控层将“连续 8 轮空源”定义为持续高危研究风险事件。  
9. 增加四项入库硬校验：存在性、非零字节、`txt/url` 配对、URL 可追溯。  
10. 常见坑：把“任务跑了”误当“证据可用”，会制造伪增量。  
11. 常见坑：空源轮次硬写交易细节，会污染后续回测基线。  
12. 合规边界：仅保留合法风控与排障建议，不提供规避监管/KYC/平台规则内容。  

如果你要，我下一步可以直接做 Round 16 的“证据链排障清单模板”（可复制到后续轮次复用）。